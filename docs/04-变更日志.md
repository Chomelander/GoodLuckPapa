# 项目变更日志（按时间倒序自动更新）

## 2026-05-08
- 修改：`js/ui/review.js` — 月度复盘用客观数据替换主观评分，新增 `computeDimensionStats()`（纯函数）和 `buildMonthlyStatsHTML()`，移除 `buildMonthlyFormHTML` / `buildMonthlySavedHTML` / `_bindMonthlySubmit`（1-5 radio 按钮 UI）
- 修改：`js/ui/review.js` — 每个维度展示两个自动计算指标：里程碑完成率（achieved/applicable）+ 观察频率（当月记录数）
- 修改：`js/ui/review.js` — 备注保存改为 note-only（不再存 scores 字段），向后兼容旧数据（旧 scores 保留但不显示）
- 修改：`tests/review.test.js` — 删除 4 个主观评分测试，新增 12 个客观统计测试（computeDimensionStats 5 + buildMonthlyStatsHTML 7）
- 验证：252/252 测试通过，Chrome DevTools UI 交互验证通过

## 2026-04-30
- 优化：`.claude/rules/03-开发测试规范.md` — 新增前端文件变更的截图验证规则（commit 前必须截图确认渲染结果）
- 新增：`~/.claude/skills/neat-freak/` skill（SKILL.md + references/sync-matrix.md + references/agent-paths.md）
- 新增：`.claude/rules/07-知识清洁规范.md` — 将 Neat-Freak 知识清洁理念编码为 Harness 规则
- 新增：`docs/harness体系使用指导/SOP-工作流速查手册.md` — 完整工作流 SOP，含 12 阶段路由、收尾流程、口令速查、架构一览
- 优化：`CLAUDE.md` 阶段 12 — 新增第 6 步调用 neat-freak 知识清洁审计
- 优化：`~/.claude/CLAUDE.md` — 新增"知识清洁纪律"全局规则
- 备注：补全 Harness v2.8 在知识生命周期管理层面的 5 个缺口（Auto Memory 无清洁机制、文档只追加不编辑、变更矩阵不覆盖记忆层、无跨项目感知、无持续健康自检）
